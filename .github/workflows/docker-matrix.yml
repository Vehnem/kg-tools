# Generated by _scripts/gen_ci.py — do not edit by hand.
name: Docker build and test

on:
  push:
    branches: [main]
  pull_request:
    paths:
      - 'tools/**'
      - '_scripts/**'
      - '.github/workflows/docker-matrix.yml'

jobs:
  docker:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        tool: ["agreementmaker", "corenlp", "dbpedia-spotlight", "falcon", "flora", "paris", "pyjedai", "stanford-openie", "valentine"]
    steps:
      - uses: actions/checkout@v4

      - name: Build and test ${{ matrix.tool }}
        run: |
          TOOL_DIR="tools/${{ matrix.tool }}/docker"
          if [ ! -d "$TOOL_DIR" ]; then
            echo "Missing $TOOL_DIR"
            exit 1
          fi
          make -C "$TOOL_DIR" docker_build
          make -C "$TOOL_DIR" docker_test || true

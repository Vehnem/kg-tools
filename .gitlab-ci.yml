stages:
  - build_and_test

paris:
  stage: build_and_test
  image: docker:24.0.5
  script:
    - apk add --no-cache bash make
    - cd _scripts
    - echo "🔨 Building paris"
    - bash build "paris"
    - echo "🧪 Testing paris"
    - bash test "paris"
  artifacts:
    expire_in: 1 week

valentine:
  stage: build_and_test
  image: docker:24.0.5
  script:
    - apk add --no-cache bash make
    - cd _scripts
    - echo "🔨 Building valentine"
    - bash build "valentine"
    - echo "🧪 Testing valentine"
    - bash test "valentine"
  artifacts:
    expire_in: 1 week

pyjedai:
  stage: build_and_test
  image: docker:24.0.5
  script:
    - apk add --no-cache bash make
    - cd _scripts
    - echo "🔨 Building pyjedai"
    - bash build "pyjedai"
    - echo "🧪 Testing pyjedai"
    - bash test "pyjedai"
  artifacts:
    expire_in: 1 week

corenlp:
  stage: build_and_test
  image: docker:24.0.5
  script:
    - apk add --no-cache bash make
    - cd _scripts
    - echo "🔨 Building corenlp"
    - bash build "corenlp"
    - echo "🧪 Testing corenlp"
    - bash test "corenlp"
  artifacts:
    expire_in: 1 week


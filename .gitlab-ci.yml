stages:
  - build_and_test

paris:
  stage: build_and_test
  image: docker:24.0.5
  script:
    - apk add --no-cache bash make
    - echo "🔨 Building paris"
    - bash _scripts/build "paris"
    - echo "🧪 Testing paris"
    - bash _scripts/test "paris"
    - echo "Pushing to registry"
    - docker tag kgt/paris 127.0.0.1:5000/kgt/paris
    - docker push 127.0.0.1:5000/kgt/paris
  rules:
    - changes:
        - paris/**/*
        - _scripts/**/*

valentine:
  stage: build_and_test
  image: docker:24.0.5
  script:
    - apk add --no-cache bash make
    - echo "🔨 Building valentine"
    - bash _scripts/build "valentine"
    - echo "🧪 Testing valentine"
    - bash _scripts/test "valentine"
    - echo "Pushing to registry"
    - docker tag kgt/valentine 127.0.0.1:5000/kgt/valentine
    - docker push 127.0.0.1:5000/kgt/valentine
  rules:
    - changes:
        - valentine/**/*
        - _scripts/**/*

pyjedai:
  stage: build_and_test
  image: docker:24.0.5
  script:
    - apk add --no-cache bash make
    - echo "🔨 Building pyjedai"
    - bash _scripts/build "pyjedai"
    - echo "🧪 Testing pyjedai"
    - bash _scripts/test "pyjedai"
    - echo "Pushing to registry"
    - docker tag kgt/pyjedai 127.0.0.1:5000/kgt/pyjedai
    - docker push 127.0.0.1:5000/kgt/pyjedai
  rules:
    - changes:
        - pyjedai/**/*
        - _scripts/**/*

corenlp:
  stage: build_and_test
  image: docker:24.0.5
  script:
    - apk add --no-cache bash make
    - echo "🔨 Building corenlp"
    - bash _scripts/build "corenlp"
    - echo "🧪 Testing corenlp"
    - bash _scripts/test "corenlp"
    - echo "Pushing to registry"
    - docker tag kgt/corenlp 127.0.0.1:5000/kgt/corenlp
    - docker push 127.0.0.1:5000/kgt/corenlp
  rules:
    - changes:
        - corenlp/**/*
        - _scripts/**/*

